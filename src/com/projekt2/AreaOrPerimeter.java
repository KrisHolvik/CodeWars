package com.projekt2;

public class AreaOrPerimeter {
    public static int areaOrPerimeter(int l, int w){
 return l == w ? l*w : 2*(l*w);
}
}
    /*{ [moje rozwiązanie, skomplikowane ;P]
       if (l == w) {
           return l*w;
           //int solution = l*w;
       } else {
           int solution = (l*2)+(w*2);
           return (l*2)+(w*2);
       }
        *//* Jeśli jest to kwadrat, zwróć jego powierzchnię.
        Jeśli jest to prostokąt, zwróć jego obwód.*//*
    }
}*/
